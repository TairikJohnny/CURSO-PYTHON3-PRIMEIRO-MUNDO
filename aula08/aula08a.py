#  import math  # importar biblioteca global
from math import sqrt  # importar função especifica

num = int(input("Digite um número: "))

raiz = sqrt(num)

print("A raiz quadrade de {} é {:.2f}".format(num, raiz))
